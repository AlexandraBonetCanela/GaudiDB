package org.gaudidb.networking;

//Handles the protocol for communication between the server and client.
//Protocol Manager: Defines a wire protocol (like MySQL’s).
public class ProtocolHandler {
}
